{
  "name": "@apm/shared",
  "version": "0.1.0",
  "description": "APM 框架无关共享层：CLI 瘦客户端与 runtime 守护进程共用的协议/客户端/配置/CLI 适配器",
  "private": true,
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "type-check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "axios": "^1.13.5"
  },
  "devDependencies": {
    "@types/node": "^20.3.1"
  }
}
